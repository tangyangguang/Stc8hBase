# 项目规则

## IoT高效推进与验证原则

- 按当前合理方案直接实施，零历史兼容、零数据迁移；不为旧测试数据保留兼容层、迁移工具或双轨实现，不扩大到凭据和无关数据清除。
- 开发交付以代码评审及本次改动相关的快速、定向检查为准：覆盖关键成功路径、明显故障与必要安全边界；通过且无阻塞问题后立即推进，不反复规划或追加无关测试。
- 长稳、soak、长时间压测、物理满容量及反复断电实验由用户在自己搭建的半生产、试运行环境中逐步验证并反馈。代理不主动执行、不等待实验窗口，也不把这些列为编码、提交或当前开发交付的前置条件；仅在用户明确要求专项实验时执行。
- 已通过且未受影响的检查直接复用；只在新改动、明确失败或证据疑点影响当前结论时重验。不得默认扩展全量/多芯片/多Core矩阵、反复重建工具链或占用机器长时间运行；纯规则/文档修改只检查内容、链接与diff。
- 效率不等于削弱TLS、重连、WDT、OTA、CRC、写入完整性或执行器安全；已知缺陷必须修复或明确停下说明。未做的运行验证如实标注，不冒称通过。本文及所引用文档中的旧验证要求若与本节冲突，以本节为准；正式发布与物理操作仍按各自授权边界执行。

- 本仓库是 STC8H 单片机项目的可复用基础库，面向资源受限的 8051 类 MCU 项目。
- 设计优先级：运行效率、ROM/RAM 低占用、行为可预期性，高于通用抽象和大而全能力。
- 当前正式目标芯片为 `STC8H1K08` 和 `STC8H8K64U-45I-LQFP48`；新增芯片支持必须基于官方资料、实际编译和硬件验证，不做运行期芯片识别。
- 目录职责：`core/` 放 MCU/编译器基础能力，`hal/` 放薄硬件抽象，`drivers/` 放可复用外设驱动，`utils/` 放小工具，`protocols/` 放轻量协议，`examples/` 放最小示例，`docs/` 放设计、验证和资料记录。
- API 保持小、明确、稳定；公共 API 不暴露编译器专属类型，不为不合理旧设计增加兼容层。
- 默认不使用动态内存、设备树、自动注册、统一外设对象表、隐藏调度器或大型框架。
- 未使用模块必须零占用：应用只编译实际使用的 `.c` 文件，模块不得在未编译或未初始化时占用 ROM、RAM、外设、中断或板级初始化。
- 高频或时序敏感路径优先使用编译期配置、板级宏或内联；避免运行期分派、函数指针表和隐藏全局缓冲。
- 新增模块、外设驱动或芯片支持前，先讨论并记录模块设计；准入标准参考 `docs/19_MODULE_ACCEPTANCE.md`。
- 编码或调试芯片、寄存器、外设、电气特性、时序和协议前，必须先查官方资料；找不到官方资料时使用权威资料，并把来源记录到 `docs/10_REFERENCES.md`。
- 高频使用且体积合理的资料可存入 `docs/vendor/`；新增本地资料必须同步更新 `docs/vendor/README.md`，并控制数量和体积。
- 唯一项目配置入口为 `core/stc8h_config.h`；板级差异通过 `STC8H_CONFIG_INCLUDE` 和 `STC8H_PINS_INCLUDE` 间接进入模块。
- 基础库源码必须同时兼容 SDCC 和 Keil C51；编译器差异集中在 `core/stc8h_compiler.h` 等 core 层，不扩散到业务模块。
- macOS 日常验证优先使用 SDCC + PlatformIO/Makefile。日常快检运行：`tools/check_examples.sh`；发布前全量示例和关键布局检查运行：`tools/check_examples_full.sh`。
- 宿主机日常单元快检运行：`tools/check_host_tests.sh`；重型 OTA/nRF24/RF-link 行为测试和高风险 IAP/地址空间编译检查运行：`tools/check_host_tests_full.sh`。
- nRF24 相关示例专项检查运行：`tools/check_nrf24_examples.sh`。
- STC8H8K64U 无硬件前置验证运行：`tools/prepare_h8k64u_validation.sh`。
- Keil C51 验证入口在 `examples/keil_c51/module_compile_check/`；真实验证需在 Windows + Keil C51 环境运行 `build_c51.bat`。
- 当前通过串口/RS485 连接且已明确识别为本项目实验台架的 ESP32、STC8H 核心板及相关模块均为可随时重烧的实验设备，其 Flash、EEPROM、IAP、NVS、校准和配置数据均无保留价值。允许为开发、诊断和验收直接烧录、擦除、重分区或重建，无需逐次说明影响或请求确认；该授权不得扩展到未来生产设备、未明确标记为实验设备的硬件或下述禁用端口。当前 STC8H8K64U Remote OTA Foundation 允许直接替换旧协议、Flash/IAP 布局、参数格式和公共 API，不提供历史兼容层或旧数据迁移。
- `/dev/cu.usbserial-110` 属于用户的其他 ESP8266 设备，不是本项目 USB-RS485；严禁打开、监听、复位、发送、烧录或以任何方式访问，除非用户以后针对该设备重新明确授权。不得按串口编号猜测设备角色；不确定的端口必须先由用户确认映射后才能访问。当前已确认 `/dev/cu.usbserial-130` 是 STC UART1/ISP 下载口，`/dev/cu.usbserial-57460296581` 是本项目 ESP32；PC USB-RS485 端口尚未确认。
- STC8H8K64U Remote OTA 必须先在本仓库完整实现并沉淀 Bootloader、IAP、协议、掉电恢复、应用链接支持和本机 PC 升级工具；下游项目只做板级配置、适配器和应用接入，不得复制、vendor、分叉、使用 Git submodule 或私有化 OTA 核心。下游应引用独立、唯一的 Stc8hBase Git 工作区并使用最新已验证提交；正式构建由下游记录实际提交以支持历史复现，通用修复必须回到本仓库实现。
- Remote OTA 必须由操作者在完整固件包和目标预检通过后，以明确命令触发；不得自动发现版本后升级，不得因上电、串口活动、联网或收到普通广播而隐式开始擦写。Application 损坏时自动停留 Bootloader 只属于恢复保护，不视为自动升级。
- 当前优先关闭 STC 端和 PC→STC 真实链路，不依赖或同步修改 ESP32/Esp32Base；ESP32 Sender/固件托管属于后续阶段。
- 完成代码或文档改动后，运行与改动范围匹配的验证；无法验证的部分必须明确说明原因和风险。
- 每个独立、可验证的功能完成后立即提交，避免长期堆积未提交改动；达到稳定节点并通过阶段 Gate 后及时推送当前开发分支，无需逐次询问。推送前检查范围，不得改写历史或强制推送。
- 每次回复用户时，说明当前是否还有剩余工作；如有，说明下一步、风险或需要用户决策的点。
